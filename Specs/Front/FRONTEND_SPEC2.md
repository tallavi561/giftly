# Giftly — מסמך אפיון Frontend, גרסה 2 (delta)

**נכון לתאריך:** 2026-08-16 · **ענף:** `build-new-architecture`
**זהו תוסף ל-[`FRONTEND_SPEC.md`](FRONTEND_SPEC.md)** (המסמך המקורי, נכון ל-2026-08-12) — לא מחליף אותו. הוא מתעד שני דברים שקרו בסשן הזה:
1. מימוש התכנון שכבר סוכם ותועד ב-FRONTEND_SPEC.md §9 ("תכנון פתוח שטרם מומש") — פיצול Home/אנשי-קשר ומסך מציאת-מתנה ייעודי.
2. שינויי UI מינימליים שנדרשו כתוצאה ישירה מהשלב הראשון של שכתוב ה-backend (ראו [`../Back/BACKEND_RECOMMENDATION_ARCHITECTURE.md`](../Back/BACKEND_RECOMMENDATION_ARCHITECTURE.md)).

**עיצוב לא השתנה** — שום טוקן צבע/גופן/רדיוס/צל לא נגע. כל מה שנבנה כאן משתמש מחדש במחלקות CSS קיימות (`.contact-row*`, `.mg-feed*`, `.btn-*`, `.empty-state`, `.page-heading` וכו') עם תוספות קטנות ועקביות בלבד (מפורטות בסוף).

---

## 1. ניווט — 4 לשוניות במקום 3

| לשונית | נתיב | לפני |
|---|---|---|
| **בית** (חדש) | `/` | לא היה קיים |
| אנשי קשר | `/contacts` (הוזז מ-`/`) | היה ב-`/` |
| לוח שנה | `/calendar` | ללא שינוי |
| הצעות בשבילי | `/my-gifts` | ללא שינוי |

אייקון "בית" הוא כרגע `redeem` (Material Symbol) — placeholder בלבד, עד שיהיה לוגו-מתנה רשמי ל-Giftly (כפי שתועד כתכנון ב-§9(1) של המסמך המקורי).

## 2. `HomePage.tsx` (חדש, `/`)

מממש את §9(1) המקורי: "אירועים ששווה להתכונן אליהם" — לראשונה בפועל, לא רק כהבטחה בכותרת:
- מציג רק אנשי קשר עם **אירוע קרוב בפועל** (לא את כל הרשימה), ממוינים לפי דחיפות (ימים עד האירוע) — נגזר מ-`api.contacts.list()` + `api.events.list()`, אותה לוגיקת `nextEventOccurrence`/`daysUntil` שהייתה ב-Dashboard הישן.
- כל שורה מקבלת גם כפתור-אייקון "מצא מתנה" (🎁, `redeem`) שקופץ ישירות למסך מציאת-המתנה הייעודי (§4 למטה), לצד הקליק הרגיל שממשיך לפתוח את `ContactPage`.
- **באנר תזכורת**: אם יש הצעות שלא נצפו/דורגו ב-batch האחרון של "הצעות בשבילי" (`api.selfRecommendations.list()`, מסונן לפי `batch_id` אחרון + `rating === null`) — מוצג באנר גרדיאנט (`--primary`→`--wordmark`) עם קישור ל-`/my-gifts`. זהו החלק השני מ-§9(1) המקורי ("תזכורת אם יש הצעות חדשות... שעדיין לא נצפו/דורגו").
- Empty state: הודעה שונה אם אין בכלל אנשי קשר (מפנה ל-`/contacts`) לעומת יש אנשי קשר אך אין אירועים קרובים.

## 3. `ContactsListPage.tsx` (היה `DashboardPage.tsx`, עבר ל-`/contacts`)

מממש את §9(2) המקורי: "רשימה טהורה — ללא אחריות על מה דחוף עכשיו":
- הוסרה לגמרי מסגרת הדחיפות: אין badge צף, אין countdown — כל שורה מציגה רק אווטאר+שם+תגית "יחס" (אם קיים).
- **נוסף שדה חיפוש** (`.search-bar` קיים) לסינון לפי שם — לקוח-בלבד (`Array.filter`), אין endpoint חדש.
- הרשימה ממוינת לפי שם (א-ב) במקום לפי סדר טעינה מה-API.
- כל שאר האחריות (מודל הוספת איש קשר, בקשות קישור נכנסות/יוצאות, פאנל היסטוריית המלצות) נשארה כאן ללא שינוי — עדיין "ניהול אנשי קשר" שייך לעמוד הזה, לא ל-Home.

## 4. `FindGiftPage.tsx` (חדש, `/contact/:id/find-gift`)

מממש את §9(4) המקורי — הכאב שתועד שם: "כדי להגיע להמלצה, המשתמש צריך לגלול למטה עד לסקשן ה-AI ואז לגלול הצידה בקרוסלה". הפתרון: מסך נפרד, בהשראת UX ה-feed האנכי של `MyGiftsPage` (אותן מחלקות `.mg-feed*` בדיוק, `scroll-snap-y`).

**נקודות כניסה:**
- כפתור "🎁" על כל שורה ב-`HomePage` (§2).
- שני כפתורי "מצא מתנה" ב-`CalendarPage` (אירועי היום + אירועים קרובים) — קופצים ישירות לכאן עם `?event=<id>`, במקום ל-`ContactPage`.
- כפתור "חפש המלצות" ב-`ContactPage` (ראו §6 — הקרוסלה הישנה הוסרה משם לגמרי, זו נקודת הכניסה היחידה מהפרופיל).

**מקור הנתונים:** `POST /api/recommendations` לבקשה ראשונה. אם לא הועבר `?event=`, העמוד בוחר לבד את האירוע הקרוב ביותר של איש הקשר; אם אין לו אירועים בכלל — מסך ריק עם קישור לפרופיל להוספת אירוע.

**פידבק בינארי "מתאים / לא מתאים" — ✅ מחובר לשרת (עודכן אחרי Phase 2):**
בהתחלה זה היה stopgap מקומי (`localStorage`) כי `PATCH /recommendations/:id/rate` עדיין לא היה קיים. עם Phase 2 של הבקאנד (ראו [`../Back/BACKEND_RECOMMENDATION_ARCHITECTURE.md`](../Back/BACKEND_RECOMMENDATION_ARCHITECTURE.md)) ה-endpoint קיים בפועל — הכפתורים קוראים ל-API האמיתי (`fit: 'FIT'|'NOT_FIT'` → `rating=5/2`), מזינים את `catalogFeedback.ts`, ומצב הכפתור נגזר מ-`rec.rating` שחוזר מהשרת (לא ממצב מקומי). `lib/localGiftRatings.ts` נמחק — לא היה עוד בשימוש.

**"עוד הצעות" — ✅ pagination אמיתי:** כפתור סוף-הפיד קורא ל-`POST /recommendations/search-more` (compute-only, לא קורא ל-Gemini שוב) במקום ליצור batch חדש. כשהשרת מחזיר `done:true` (תקרת 30 לכל session, spec §6.3) — הכפתור נעלם ומוצגת הודעה מתאימה במקומו.

## 5. `MyGiftsPage.tsx` — תיקון הכרחי, לא רק שיפור

**זה לא היה אופציונלי:** אחרי Phase 1 של שכתוב ה-backend, `PATCH /self-recommendations/:id/rate` **דורש** `feedback_reason` על כל דירוג של 3 ומטה (החזרת 400 אחרת) — וה-UI הקיים שלח רק `{rating}`. בלי התיקון הזה, דירוג נמוך היה נשבר בפועל.

נוסף picker קטן של 3 צ'יפים (`WRONG_CONCEPT` / `WRONG_PRODUCT` / `TOO_GENERIC`, עם ניסוח אנושי) שמופיע מתחת לכוכבים ברגע שנבחר דירוג 1–3, ורק בבחירת סיבה בפועל נשלחת הבקשה לשרת (עד אז ה-UI שומר את הדירוג כ"ממתין" מקומית, לא שולח כלום). דירוג 4–5 ממשיך להישלח מיידית כמו קודם, ללא שאלה.

## 6. `ContactPage` — סקשן ה-AI פושט לכפתור בודד (בקשה נפרדת מהמשתמש)

הקרוסלה האופקית של המלצות (עם התמונות/גרדיאנטים) **הוסרה לגמרי** מפרופיל איש הקשר — אין יותר ייצור/הצגת המלצות inline שם. הסקשן נשאר רק ככותרת+תת-כותרת (כולל ה-chip "מסונן לפי תקציב" כשנבחר אירוע) וכפתור בודד **"חפש המלצות"** (מושבת עד שנבחר אירוע) שמנווט ישירות ל-`find-gift`. גם `RecommendationCarousel.tsx` נמחק — לא נשאר לו עוד קורא. הפרופיל עכשיו רק *מפנה* לחוויה, לא מנסה לשכפל אותה.

## 7. שינויים קטנים נוספים

- `ContactPage`: כפתור החזרה (`chevron_right`) עבר מ-`navigate('/')` הקשיח ל-`navigate(-1)` — כי עכשיו יש שתי נקודות כניסה אפשריות (Home או אנשי קשר), ו-`-1` תמיד נכון.
- `AppShellLayout`: `NAV_ITEMS` קיבל את הפריט הרביעי; `isActive('/')` כבר טיפל נכון בהתאמה מדויקת (לא `startsWith`), כך שלא היה צריך לגעת בלוגיקה עצמה.

## 7. מחלקות CSS חדשות (תוספתיות בלבד)

| מחלקה | שימוש |
|---|---|
| `.contact-row-trailing` | עוטף את ה-badge+כפתור-מתנה בשורת איש קשר ב-Home, כדי לשמר את פריסת ה-`space-between` הקיימת |
| `.home-find-gift-btn` | כפתור עגול קטן, `--primary-container` — אותו דפוס עיצובי כמו `.btn-icon-sm` |
| `.home-suggestions-banner` | גרדיאנט `--primary`→`--wordmark`, תואם ל-`.ai-banner` הקיים |
| `.mg-feedback-reasons` / `.mg-feedback-reason-chip` | באותו סגנון shorthand-chip כמו `.mg-feed-search` |
| `.find-gift-back-btn` / `.find-gift-header-label` | overlay כהה שקוף על גבי הפיד, תואם ל-scrim הקיים |
| `.find-gift-fit-actions` / `.find-gift-fit-btn` (`.fit`/`.not-fit`) | מבנה זהה ל-`.mg-star-rating`, `.fit.active` משתמש ב-`--tertiary` (רוז) הקיים לאירועים דחופים |

אין טוקן חדש ב-`:root`, אין גופן/צבע חדש.

## 8. מה עדיין לא נגעתי בו (מכוון)

- **"קבוצות למתנה"** — עדיין לא מתוכנן ברמת מנגנון, בדיוק כמו שתועד ב-§9(5) המקורי.
- **שכבת שיתוף אירועים/קבוצות** (spec §14 של ה-backend) — אין לה שום frontend, גם לא שלד. תבוא ביחד עם Phase 5 של הבקאנד.
- **תמונות מוצר אמיתיות** — עדיין gradient לפי קטגוריה בכל מקום (`gradientForCategory`), אין `image_url` מאוכלס.
- **תקרת ה-30 לכל session** מוצגת רק כהודעה גנרית בסוף הפיד (`done:true`) — אין מונה "X/30" חזותי במהלך הגלילה.
- **`GET /recommendations`** לא חושף עדיין `rate_limit`/מכסה שבועית שנותרה בממשק (רק ב-`POST` הראשון, כהודעת שגיאה אם עברת את המכסה) — משתמש לא רואה "נשארו לך 3 חיפושים השבוע" מראש.
